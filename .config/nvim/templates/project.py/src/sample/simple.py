def add_one(number: int) -> int:
    """Increments a number.

    Example:
    >>> add_one(10)
    10

    Args:
        number (int): The number to increment.

    Returns:
        int: The incremented number.
    """
    return number + 1
